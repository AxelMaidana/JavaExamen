package Excepciones;
public class excepciones {
    public static void main(String[] args) {
        try {
            // Código que podría generar una excepción
        } catch (Exception e) {
            // Código para manejar la excepción
        } finally {
            // Código que siempre se ejecutará (opcional)
        }
    }
}